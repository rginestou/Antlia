#version 330
layout (location=0) in vec3 position;
layout (location=1) in vec4 color;
out vec4 outColor;
uniform mat4 viewport;

void main()
{
    gl_Position = viewport * vec4(position, 1);
    outColor = color;
}
